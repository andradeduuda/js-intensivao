const minhaVariavel = ("duda"); // Altere o valor dessa variável para testar

if (typeof minhaVariavel === 'string') {
  console.log('É uma string');
} else {
  console.log('Não é uma string');
}
